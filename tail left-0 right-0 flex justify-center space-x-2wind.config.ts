import index) => (
          <button
            key={index}
            onClick={() => setCurrentSlide(index)} type { Config } from "
            className={`w-2 h-2 rounded-full transitiontailwindcss";

export default {
  darkMode: ["class"],
  content: [-all ${
              index === currentSlide
    "./pages/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}",
    "./app/**/*.{ts,tsx}",
    "./src/**/*.{ts,tsx}" ? 'bg-white w
  ],
  prefix-6' : 'bg: "",
  theme: {
    container: {
      center: true,
      padding: '-white/50'
            }`}
            aria-label={`2rem',
      screens: {
        'sm': '640px',
        'md':Перейти к слай '768px',
        'lg': 'ду ${index + 1}`}
          1024px',
        'xl': '/>
        ))}
      </div>
    </section>
  );
};1280px',
        '2xl':

export default HeroSection;</pp-content '1400px'
      }
    >
</pp-code>

<pp},
    fontFamily: {
      -code>
<pp-filepath>srcsans: ['Helvetica/components/home/BrandFe Neue', 'Arial', 'sans-atures.tsx</pp-filepath>
<ppserif'],
      futura: ['Fu-content>import { Batura PT', 'Helvetica Neue',dgeCheck, Gem, Lock 'Arial', 'sans-serif'],, Truck } from 'lucide-react';

const features = [
  {
    id: 
    },
    extend:1,
    icon: <BadgeCheck className="h-8 w-8" {
      colors: {
        border />,
    title: "Универ: 'hsl(var(--border))',сальность",
    description
        input: 'hsl(var: "Наши украшения ид(--input))',
        ring: 'hsl(var(--ring))',еально вписываются в люб
        background: 'hsl(varой образ и стиль жизни(--background))',
        foreground: 'hsl(var(--foregroun"
  },
  {
    id:d))',
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'h 2,
    icon: <Gemsl(var(--primary-foreground) className="h-8 w-8" />,
    title: "Кач)'
        },
        secondary: {
          ество",
    description: "Используем только высококачественные материалыDEFAULT: 'hsl(var(--secondary))',
          foreground: ' и ручную работу"
  },
  {
    id: hsl(var(--secondary-foregroun3,
    icon: <Lock className="d))'
        },
        destructh-8 w-8" />,ive: {
          DEFAULT: 'h
    title: "Эксклюзивность",
    description: "Каждое изделие уникальноsl(var(--destructive))', и выполнено с в
          foreground: 'hsl(var(--destructive-foreground))'
        },
        muted: {
          ниманием к деталям"
  DEFAULT: 'hsl(var(--muted))',
          foreground: },
  {
    id: 4'hsl(var(--muted-foreground))'
        },
        accent,
    icon: <Truck className=": {
          DEFAULT: 'hsl(var(--accent))',
          foreh-8 w-8" />,ground: 'hsl(var(--
    title: "Быстрая доставaccent-foreground))'
        },ка",
    description: "Доставляем по всей России в течение
        popover: {
          DEFAULT: 'h 2-3 рабочих днейsl(var(--popover))',"
  }
];

const Bran
          foreground: 'hsl(vardFeatures = () => {
  return (
    <section className="py(--popover-foreground))'
        },
        card: {
          DEFAULT: 'hsl(var(--card))',
          foreground: 'h-20 bg-alterissl(var(--card-foreground)-white">
      <div)'
        },
        silver: {
          DEFAULT className="container mx-auto px-4">: 'hsl(var(--silver
        <h2 className="text-3xl font-futura))',
          light: 'hsl text-center mb-16">Наши преимущества</h2>(var(--silver-light))',
        
        <div className="grid grid-cols-1 md:gri
          dark: 'hsl(var(d-cols-2 lg:grid---silver-dark))'
        },cols-4 gap-8">
          
        alteris: {
          {features.map((feature) => (
            <div key={feature.id} classNamewhite: 'hsl(var(--alter="flex flex-col items-center text-center p-6 borderis-white))',
          gray: 'hsl(var(--alteris-gray))', border-alteris-silver-light hover:border-alteris-silver transition
          black: 'hsl(var(--alteris-black))'-colors duration-300">
              
        }
      },
      border<div className="text-alteris-blackRadius: {
        lg: 'var(--radius mb-4">
                {feature.)',
        md: 'calc(varicon}
              </div>
              <h3 className="text-xl font-(--radius) - 2px)',futura mb-3">{feature.title
        sm: 'calc(var(--radius) - 4px)'
      },}</h3>
              <p className="
      keyframes: {
        'accordiontext-alteris-gray-dark">{feature-down': {
          from: { height.description}</p>
            </div>
          ))}
        </div>: '0' },
          to: { height:
      </div>
    </section>
  );
};

export default BrandFe 'var(--radix-accordion-atures;